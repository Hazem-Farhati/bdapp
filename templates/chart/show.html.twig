{% extends 'base.html.twig' %}

{% block title %}Chart{% endblock %}

{% block body %}
    <h1>Chart</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ chart.id }}</td>
            </tr>
            <tr>
                <th>Number</th>
                <td>{{ chart.number }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ chart.date ? chart.date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('chart_index') }}">back to list</a>

    <a href="{{ path('chart_edit', {'id': chart.id}) }}">edit</a>

    {{ include('chart/_delete_form.html.twig') }}
{% endblock %}
